% Generated by roxygen2: do not edit by hand
\name{geom_timeline_label}
\alias{geom_timeline_label}
\title{Timeline label}
\usage{
geom_timeline_label(mapping = NULL, data = NULL, stat = "identity",
  position = "identity", na.rm = FALSE, show.legend = FALSE,
  inherit.aes = TRUE, ...)
}
\arguments{
\item{mapping}{Set of aethetics created by aes.}

\item{data}{A data.fram such as that obtained from NOAA}

\item{stat}{A string of statistical transfromation.}

\item{position}{Position adjustment.}

\item{na.rm}{A logical expression for dealing with missing values.}

\item{show.legend}{A logical expression for showing legend or not.}

\item{inherit.aes}{A logical expression of whether or not override default aesthetics.}

\item{...}{Other arguments passed to layer.}
}
\description{
Timeline label
}
\details{
Adds labels for the locations of individual earthquakes. Used with geom_timeline.
}
\examples{
\dontrun{
# load and clean data
file_path <- system.file("data", "dt.tsv", package = "PCR")
dt <- read_tsv(file_path) \%>\%
  eq_clean_date \%>\%
  eq_clean_location

# make graph
dt \%>\%
filter(COUNTRY == 'MEXICO') \%>\%
  ggplot(aes(date = date,
             xmin = as.Date('1995-01-01'),
             xmax = as.Date('2000-12-30'),
             y = COUNTRY,
             colour = DEATHS,
             fill = DEATHS,
             size = EQ_PRIMARY,
             location = LOCATION)) +
  geom_timeline() +
  geom_timeline_label() +
  theme(axis.line.y = element_blank(),
        axis.line.x = element_line(),
        axis.ticks.y = element_blank(),
        axis.text.y = element_blank(),
        axis.title.y = element_blank(),
        legend.position = 'bottom',
        panel.grid = element_blank(),
        panel.background = element_blank())
}
}
